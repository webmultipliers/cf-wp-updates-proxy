# Changelog

All notable changes to this project should be documented in this file.

The project is distributed as a fork-first harness: consumers sync from
upstream to receive updates.

## [Unreleased]

### Changed
- Distribution guidance is now fork-first (instead of template-first).
- README now documents a "Staying updated" sync flow for forks.
- Deploy workflow push trigger includes both `main` and `development`.
